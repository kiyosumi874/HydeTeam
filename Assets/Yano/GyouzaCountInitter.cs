using System.Collections;
using System.Collections.Generic;
using UnityEngine;
/// <summary>
/// 餃子の数を0にするスクリプト
/// </summary>
public class GyouzaCountInitter : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GyouzaCounter.Init();
    }
}
